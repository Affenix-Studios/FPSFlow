# Done to increase the memory available to the Gradle daemon.
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m

# Fabric Properties
# Variant coordinates are defined in build.gradle.kts.
# These values are kept as the stable default for tooling that reads properties directly.
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.1
loader_version=0.19.3
fabric_version=0.141.4+1.21.11

# Mod Properties
mod_version=1.8.4
modmenu_version=17.0.1-beta.1
maven_group=dev.fpsflow
archives_base_name=fpsflow

# Default Java target for single builds.
# Running `gradle clean build` will build the stable 1.21.11 target by default.
java_version=21
build_target=legacy
